# wasm-pack build output — rebuilt locally (build.sh) and in CI (pages.yml).
/pkg/
/assets/
/demo.docx
/sample.docx

# Playwright run artifacts (test-results/, HTML report) — regenerated per run.
/playwright-report/
/test-results/
