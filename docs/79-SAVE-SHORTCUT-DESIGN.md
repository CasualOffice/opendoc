# Save shortcut

`Ctrl+S` on Windows/Linux and `Cmd+S` on macOS invoke the existing user
initiated DOCX export/download path. The browser's default save-page action is
prevented only when a document is open; without a document the shortcut is
left untouched. Export errors remain visible in the status bar.
