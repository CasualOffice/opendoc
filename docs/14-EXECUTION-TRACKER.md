# Execution Tracker

## Purpose

This tracker records project execution state. It is intentionally lightweight at the beginning and should become more detailed as implementation starts.

Update this file whenever work begins, changes scope, or finishes.

## Status Values

- Not started
- Researching
- Designing
- Finalizing
- Ready
- In progress
- Blocked
- In review
- Done

## Foundation Tracker

| ID | Workstream | Status | Acceptance Gate | Notes |
| --- | --- | --- | --- | --- |
| F-001 | Repository bootstrap docs | Done | Root docs, agent docs, license, process docs added. | Initial setup complete. |
| F-002 | Project glossary | Done | Common terms defined and linked from README. | `17-GLOSSARY.md`. |
| F-003 | Support matrix | Done | OS/WASM/headless targets documented. | `18-SUPPORT-MATRIX.md`. |
| F-004 | CI design | Done | Required checks and platforms documented. | `15-CI-AND-RELEASE-GATES.md`. |
| F-005 | Workspace scaffold design | Done | Rust workspace layout finalized. | `19-WORKSPACE-SCAFFOLD-DESIGN.md`. |
| F-006 | Error code registry | Done | Stable initial error taxonomy. | `20-ERROR-CODE-REGISTRY.md`. |
| F-007 | Parser limits spec | Done | ZIP/XML/image/package limits specified. | `21-PARSER-LIMITS.md`. |
| F-008 | Normalized schema v0 design | Done | Model primitives and serialization draft accepted. | `22-NORMALIZED-SCHEMA-V0.md`. |
| F-009 | DOCX fixture corpus plan | Done | Corpus manifest format and source policy defined. | `23-DOCX-FIXTURE-CORPUS.md`. |
| F-010 | Competitive analysis pass 1 | Done | Findings recorded in `12-COMPETITIVE-ANALYSIS.md`. | Primary sources checked 2026-07-24. |
| F-011 | Phase 1 capability decomposition | Done | Phase 1A through 1D have independent scope and exit gates. | ADR-025 and `06-ROADMAP-AND-DELIVERY.md`. |
| F-012 | Apache-2.0 license policy | Done | Apache-2.0 metadata, text, fixtures, and contribution terms agree. | ADR-026. |
| P0-001 | Deterministic model transaction slice | Done | Blank document, grapheme-aware insertion, atomic transaction, snapshots, and tests. | Native, WASM, MSRV, docs, lint, and policy gates pass. |
| P0-002 | Transaction semantics and history | Done | Insert, delete, split, join, mapping, inverse, and history semantics accepted and implemented. | 17 unit tests plus SDK doc test; native/WASM/MSRV gates pass. |
| P0-003 | Normalized snapshot loading | Done | Strict schema v0 JSON load, validation, limits, and deterministic round trip. | 25 unit tests plus SDK doc test; native/WASM/MSRV gates pass. |
| P0-004 | Selection foundation | Done | Caret/range invariants and position mapping implemented. | 31 unit tests plus SDK doc test; native/WASM/MSRV gates pass. |
| P0-005 | Runtime event foundation | Done | Ordered transaction and selection events with safe subscription lifecycle. | 36 unit tests plus SDK doc test; bounded lag and atomic failure gates pass. |
| P0-006 | DOCX package reader | Done | Security-bounded ZIP admission, metadata, part reads, and generated package fixtures. | 44 unit tests plus SDK doc test; native/WASM/MSRV and parser-policy gates pass. |
| P0-007 | Benchmark and baseline harness | Done | Reproducible package/model timing, reports, and regression thresholds. | 50 unit tests plus SDK doc test; baseline comparison, native/WASM/MSRV, and CI smoke gates pass. |
| P0-008 | Phase 0 corpus and evidence closure | Done | Seven generated fixtures, fuzz infrastructure, and linked exit evidence. | Full acceptance matrix passed; see `31-PHASE-0-EXIT-REPORT.md`. |

## Maintenance

| ID | Title | Owner | Status | Notes |
| --- | --- | --- | --- | --- |
| M-001 | Decompose god-files into modules | Claude Code | Done | Owner directive: no god files — complete. `model/v1.rs`→`v1/`; `casual-doc-import`→7 modules; and (parallel multi-agent worktree decomposition, merged) `model/lib.rs` (v0)→`ids`/`body`/`extension`/`document`/`snapshot`/`error`, `ooxml/lib.rs`→`limits`/`archive`/`path`/`relationships`/`contenttypes`/`discovery`/`package`/`error`, `sdk/lib.rs`→`value`/`selection`/`snapshot`/`command`/`event`/`config`/`session`/`error`. Every crate root is now a ≤64-line module wiring file; public APIs preserved via re-exports; all workspace gates green. |
| M-003 | Fix casual-doc-import review findings (13 confirmed) | Claude Code | In review | 12/13 fixed. Blockers: basedOn cycle broken; styles-part unmapped now reported; char-level rPr spacing reported (context-aware reporting). Majors: sz bounded; style-ref kind check; nested rPr/pPr depth counters; outside-body constructs reported; CDATA captured; report-feature count capped with overflow bucket. Minors: pPr/rPr marks + out-of-context handled elements reported. Remaining (minor, deferred): degraded-attribute detail (e.g. `w:u val="double"`) mapped-but-lossy needs a Degraded disposition. Landed with the importer decomposition. | Adversarial review found 13 verified defects. Blockers: (a) basedOn cycle aborts import; (b) styles-part unmapped constructs never reported (report gated on in_body); (c) char-level w:spacing in rPr silently dropped (HANDLED name collision). Majors: nested rPr/pPr premature context exit drops formatting; w:sz out-of-domain aborts whole import; rStyle/pStyle bind without kind check; constructs outside body unreported; CDATA text dropped; report-entry count uncapped. Minors: pPr/rPr marks unreported; degraded-attribute detail unreported; out-of-context handled elements unreported. Fix + regression tests, then decompose the importer with corrected context-aware reporting. |
| M-002 | LibreOffice differential fidelity harness | Claude Code | In review | `tools/opendoc-fidelity` binary: extracts document text via the OpenDoc importer and via `soffice --headless --convert-to txt`, normalizes whitespace, strips generated list markers, and reports PASS/DIFF + a **word-multiset match %**. Word-level (not line-level) comparison is insensitive to how a producer groups text into lines — LibreOffice joins a table row's cells onto one line while the importer emits one line per cell — so tables score their true content fidelity while a genuine gap still shows. Current corpus: 6/7 at 100% (paragraphs, unicode, hyperlinks, tables incl. nested + merges); footnotes 93% — the one real remaining LibreOffice-visible text gap is footnote-part body text (pending slice). The round-trip-fidelity proxy until the Phase-2 writer exists. Not a CI dep. |

## Active Work

| ID | Title | Owner | Status | Links |
| --- | --- | --- | --- | --- |
| P1A-001 | Semantic DOCX import design | Codex | Accepted | `32-…`; accepted (architecture-level) 2026-07-24 via ADR-027. Importer code gated on schema v1. |
| P1A-002 | DOCX engine competitor source study | Codex | Done | `33-DOCX-ENGINE-COMPETITOR-RESEARCH.md`; extended by `37-PHASE-1A-DECISION-RESEARCH.md`. |
| P1A-003 | OOXML fidelity architecture | Codex | Accepted | `34-…`; ADR-027 **accepted** 2026-07-24 (`36-ADR-027-ACCEPTANCE-RECORD.md`). |
| P1A-005 | Read path: relationship-based main-document discovery (R1) | Claude Code | In review | Implemented in `casual-doc-ooxml`: admitter now requires only `[Content_Types].xml` + `_rels/.rels`; bounded `quick-xml` parse of `_rels/.rels` + `[Content_Types].xml`; `officeDocument` discovery (transitional+strict), root-relative target resolution with escape rejection, content-type binding, fail-closed typed errors (`MissingMainDocument`/`AmbiguousMainDocument`/`UnsupportedMainDocumentType`/`MalformedPackageXml`); `main_document_part()` accessor. Fixtures regenerated to valid OPC; 5 new tests; all gates green (fmt, clippy, test, wasm, MSRV, doc, deny, fuzz-lock, benchmark smoke). |
| P1A-006 | Deterministic import ID/namespace seed (R3) | — | Not started | Input-derived, order-independent seed; reordered-ZIP + native/WASM golden. |
| P1A-021 | Model inline drawings + hyperlinks (first semantic-extension slices) | Claude Code | In review | Implemented the drawing + hyperlink slices of P1A-019's verified blueprint. **Model** (`casual-doc-model`): additive `InlineNode::Drawing{media: MediaId, extent?: Extent}` and `InlineNode::Hyperlink{target, tooltip?, inlines}` (`HyperlinkTarget` External{url}/Internal{anchor}); MAX_EMU public; two `ModelError` variants (EmptyHyperlink, NestedHyperlink); `validate_body`→recursive `validate_inlines(in_hyperlink)`, id-uniqueness + snapshot-limits recurse into hyperlink children; strictly additive (existing v1 byte-identical, migration golden unchanged). **Import** (`casual-doc-import`): media built before body → `MediaId` index; `w:drawing`→Drawing (embedded-picture only; embed-less/dangling/degraded reported; scoped scaffolding suppression, non-scaffolding still reported); `w:hyperlink`→Hyperlink via `push_segment` router + `hyperlink_depth` nesting; `ParseInputs` struct. Fidelity harness recurses hyperlink text (100% on real fixtures). 18 tests (10 model + 6 import + 2 export model-presence on real docs). All 6 adversarial-verification fixes folded in. Gates green. Remaining P1A-019 slices: tables, fields, headers/footers (specs to re-run). |
| P1A-022 | Model semantic tables (structure + cell-merge geometry) | Claude Code | In review | Second structural slice of P1A-019 (`39-SCHEMA-V1-TABLES-DESIGN.md`, accepted 2026-07-25). **Model** (`casual-doc-model`): additive `BlockNode::Table(Table)` with `grid: [GridColumn]`, `rows: [TableRow]`, cells holding recursive `blocks: [BlockNode]` (paragraphs + nested tables); `TableCellProperties{grid_span, vertical_merge, width_twips}`; `VerticalMerge{Restart,Continue}`; `MAX_TABLE_DEPTH=32`; four `ModelError` variants (Empty{Table,TableRow,TableCell}, TableNestingTooDeep). `validate_body`/`validate_unique_ids`/`validate_snapshot_limits` recurse through tables (depth bound, id-uniqueness for table/row/cell, block-count + text limits count nested nodes). Strictly additive — v0→v1 migration and the byte-exact golden unchanged (v0 has no tables). **Import** (`casual-doc-import`): new `tables.rs` `TableStack` builder; `body.rs` maps `w:tbl`/`w:tr`/`w:tc`/`w:tblGrid`/`w:gridCol` and `w:tcPr` → `w:gridSpan`/`w:vMerge`/`w:tcW` (dxa only; pct/auto reported); cell paragraphs route into the open cell (replacing the R4 flatten); nested tables push into their parent cell; every unmapped table construct (tblPr/trPr/borders/…) still reported — no silent loss. Fidelity harness + export helper recurse cells. ~24 new tests (9 model + rewritten + import + 2 depth/malformed-nesting regressions). All gates green (fmt, clippy, 149 tests, doctest, wasm, MSRV 1.85, doc). **Adversarial multi-agent review** (5 dimensions → verify) confirmed 1 real defect: an over-`MAX_TABLE_DEPTH` table refused by `open_table` left `is_active()` true so its subtree mutated the parent table (silent corruption); fixed with a context-independent, always-balanced `suppressed_tbl_depth` counter (every `<w:tbl>` opens-or-suppresses, every `</w:tbl>` balances) — a follow-up single-agent re-review found and closed a residual start/end-asymmetry desync on malformed `<w:tbl>`-inside-`<w:p>`; both covered by regression tests. Remaining P1A-019 slices: fields, headers/footers, tracked changes. |
| P1A-023 | Model semantic fields (instruction + cached result) | Claude Code | In review | Fields slice of P1A-019 (`40-SCHEMA-V1-FIELDS-DESIGN.md`, accepted 2026-07-25). **Model** (`casual-doc-model`): additive `InlineNode::Field{id, instruction, inlines}` (opaque instruction, non-empty ≤`MAX_FIELD_INSTRUCTION_BYTES`=4096; cached-result inlines may be empty, leaf-only); `validate_inlines` generalizes `in_hyperlink`→`in_wrapper` so Hyperlink and Field are the two inline wrappers and a wrapper-in-wrapper is rejected (`NestedField`/`NestedHyperlink`); id-uniqueness + limits recurse into the result, instruction bytes bounded. Additive — v0→v1 migration + golden unchanged. **Import** (`casual-doc-import`): `w:fldSimple` and complex `fldChar` begin/instrText/separate/end run-sequence machine (`FieldAccumulator` + balanced `field_depth` + `in_instr` instruction buffer); fields and hyperlinks mutually exclusive (wrapper-in-wrapper reported + flattened, no display-text loss); missing-`end` flushed at paragraph close; commit rejects empty/oversize instruction by flattening the result. Fidelity harness + export walker recurse field results. 15 new tests (7 model + 8 import incl. malformed/nesting/regressions). All gates green (fmt, clippy, 164 tests, doctest, wasm, MSRV 1.85, doc). **Adversarial multi-agent review** (4 dimensions → verify) confirmed 2 real defects, both fixed: (1) major — `push_segment` silently dropped display runs that arrived while a field was collecting its instruction (missing/duplicated `separate`); now every field display segment is captured into the cached result (lossless). (2) minor — `instrText` with no field collecting it was dropped silently; now reported. Both covered by regression tests. Remaining P1A-019 slices: headers/footers, footnotes, tracked changes. |
| P1A-025 | Importer no-skip audit (text boxes, VML, extra parts, containers) | Claude Code | In review | Multi-agent audit (4 dimensions → adversarial verify) of every silently-skipped/mis-captured construct in the importer: **16 confirmed** findings clustered as (1) text boxes — `w:txbxContent` mis-captures boxed text into the enclosing paragraph, truncates it, and silently drops the enclosing drawing's image (blocker); (2) `mc:AlternateContent` walks both branches → duplicated content (blocker); (3) header/footer/footnote/endnote/comments parts never read in Semantic mode → text/images/hyperlinks silently absent, reference ids dropped (blocker/major); plus `w:ruby` reorder (minor). Drives P1A-024 (text boxes, done here) and the extra-parts + VML-image + ruby slices next. |
| P1A-024 | Model text boxes + AlternateContent selection | Claude Code | In review | Text-box slice of P1A-019 (`41-SCHEMA-V1-TEXTBOXES-DESIGN.md`, accepted 2026-07-25), fixing P1A-025 clusters 1–2. **ooxml**: generalized `resolve_main_document_relationships`→`resolve_part_relationships` + public `DocxPackage::part_relationships(part)` so any part (header/footer/footnote) resolves its own image/hyperlink refs (foundation). **Model** (`casual-doc-model`): additive `InlineNode::TextBox{id, blocks}` (non-empty; reuses the recursive block model), `MAX_TEXTBOX_DEPTH=8`, `EmptyTextBox`/`TextBoxNestingTooDeep` errors; validate/id-uniqueness/limits recurse and thread a text-box depth. **Import** (`casual-doc-import`): `w:txbxContent` parsed in a suspended `ContentFrame` (save/restore ~28 content fields) so the box's inner paragraphs build a fresh context — the enclosing paragraph and the enclosing drawing's image stay intact; `mc:AlternateContent` selects one branch (others skipped+reported via `mc_skip_depth`). Fidelity harness + export walker recurse boxes. 13 new tests (5 model + 5 import incl. the 3 audit-blocker regressions + 1 ooxml + 2 review regressions). All gates green (fmt, clippy, 175 tests, doctest, wasm, MSRV 1.85, doc). **Adversarial review** (3 dimensions → verify) confirmed 5 findings; 2 real bugs fixed: (major) importer gave each text box a fresh table stack but the model threaded the enclosing table depth into box blocks → deep tables + a box of tables aborted the whole import; the model now restarts the table budget per box (matching the importer). (minor) a `w:sectPr` inside a text box pushed a phantom document section; now guarded to the true body level. The review also caught that the import regression tests had been reverted (re-added). Remaining minor (accepted): text-box id allocated eagerly (consistent with tables). |
| P1A-026 | Model footnotes/endnotes (extra-part bodies) | Claude Code | In review | Notes slice of P1A-019 (`42-SCHEMA-V1-NOTES-DESIGN.md`, accepted 2026-07-25), closing the P1A-025 note-part silent-drop + the last LibreOffice-visible text gap. **Model** (`casual-doc-model`): additive `Definitions.footnotes`/`endnotes: DefinitionMap<NoteId, Note>` (empty-omitted so existing snapshots + migration golden are byte-identical), `Note{blocks}`, `InlineNode::NoteReference{id, kind, note}`, `DanglingNoteRef` error; `validate_notes` (fresh depth), kind-correct resolution, id-uniqueness + limits recurse into notes. **Import** (`casual-doc-import`): `import_package` reads `/footnotes`+`/endnotes`; `body::parse_notes` runs the body parser in note-container mode (each `w:footnote`/`w:endnote` a block container keyed by `w:id`, NoteId in document order; separator/continuation notes skipped+reported); `build_notes` maps `w:id`→NoteId; body `w:footnoteReference`/`w:endnoteReference` resolve to a `NoteReference` (dangling reported). Note-part media/hyperlinks reported (modeling = follow-up). Fidelity harness appends note body text (+ strips footnote auto-number markers). 12 new tests (4 model + 5 import incl. 2 review regressions). All gates green (fmt, clippy, 184 tests, doctest, wasm, MSRV 1.85, doc). **Adversarial review** (3 dimensions → verify) confirmed 3 malformed-input silent-loss holes, all fixed: (major) `close_note` finished the paragraph before unwinding text-box frames, dropping an outer paragraph a text box restored — now unwinds frames first (same fix mirrored in the body `parse` unwind); (minor) a stray `<w:body>` and a body-level `w:sectPr` inside a notes part were modeled-then-discarded — now gated to body mode and reported. Footnote content now modeled (remaining fixture DIFF is LibreOffice auto-numbering, not content). |
| P1A-027 | Model headers/footers (extra-part bodies + section refs) | Claude Code | In review | Headers/footers slice of P1A-019 (`43-SCHEMA-V1-HEADERS-FOOTERS-DESIGN.md`, accepted 2026-07-25), closing the P1A-025 header/footer silent-drop. **Model** (`casual-doc-model`): additive `Definitions.headers`/`footers: DefinitionMap<HeaderFooterId, HeaderFooter>` (empty-omitted), `HeaderFooter{blocks}`, `SectionBoundary.headers`/`footers: Vec<HeaderFooterRef>` (additive; empty sections byte-identical), `HeaderFooterRef{kind: Default/First/Even, reference}`, `DanglingHeaderFooterRef`; `validate_headers_footers` (fresh depth), section-ref resolution in the correctly-named map, id-uniqueness + limits recurse. **Import** (`casual-doc-import`): `import_package` reads each `/header`+`/footer` part (keyed by `r:id`); `body::parse_header_footer` runs the body parser in single-container mode (`w:hdr`/`w:ftr` root); `build_header_footers` allocates a HeaderFooterId (document order) per part; `w:sectPr` `w:headerReference`/`w:footerReference` (`r:id`+`w:type`) resolve to `HeaderFooterRef` (dangling reported). Header/footer-part media/hyperlinks reported (follow-up). 11 new tests (3 model + 7 import incl. a real-fixture end-to-end + 1 review regression). All gates green (fmt, clippy, 192 tests, doctest, wasm, MSRV 1.85, doc). **Adversarial review** (3 dimensions → verify) confirmed 3 findings (one root cause): the note-mode `no-silent-loss` guards on the `w:body`/`w:sectPr` arms weren't extended to header/footer (`hf_root`) mode, so a stray `w:body`/`w:sectPr` in a header/footer part was silently consumed (and a phantom section burned an id) — fixed by also requiring `hf_root.is_none()`, regression-tested. |
| P1A-028 | Model legacy VML pictures (`w:pict`) | Claude Code | In review | VML-images slice of P1A-019 (`44-SCHEMA-V1-VML-IMAGES-DESIGN.md`, accepted 2026-07-25), closing the P1A-025 VML-image reported-but-dropped finding. **No model change** — a VML picture reuses `InlineNode::Drawing` (extent `None`; VML sizes in CSS). **Import** (`casual-doc-import`): `w:pict` opens a picture context (`pict_depth`, saved/restored across text-box frames); `v:imagedata@r:id` resolves through the same media index as DrawingML; `commit_pict` emits a `Drawing` for a resolvable id, else reports (an image-less shape / VML text box is handled by the text-box slice). 2 new import tests. All gates green (fmt, clippy, 194 tests, doctest, wasm, MSRV 1.85, doc). **Focused adversarial review** confirmed the 6 mandated scenarios correct and found 1 low-severity parity gap: `finish_paragraph` reset the drawing counters but not `pict_depth` (a latent leak, essentially unreachable under quick-xml `check_end_names` but a data-loss trap once extra-part media lands) — fixed with a one-line reset matching the drawing handling. |
| P1A-029 | Model media/hyperlinks inside notes/headers/footers | Claude Code | In review | Extra-part media/links slice of P1A-019 (`45-EXTRA-PART-MEDIA-AND-HYPERLINKS-DESIGN.md`, accepted 2026-07-25), closing the P1A-025 extra-part image/link silent-drop. **No model change** — reuses `Drawing`/`Hyperlink`/`MediaReference`. **Import** (`casual-doc-import`): `media::build` aggregates image relationships across the whole package (main → footnotes → endnotes → headers → footers), de-duped by resolved image part (shared image = one `MediaId`), returning a `part_name→MediaId` resolver; `media::part_index` builds each part's own `rId→MediaId` index. `PartSources{xml, images, hyperlinks}` per extra part (resolved in `import_package` via `part_relationships`); `parse_notes`/`parse_header_footer` now receive their part's media index + external-hyperlink map (previously empty). Per-part relationship ids resolve independently. 3 new import tests (image-in-footnote, external-link-in-header, backward-compat two-refs-one-image). All gates green (fmt, clippy, 197 tests, doctest, wasm, MSRV 1.85, doc). **Adversarial review** (3 dimensions → verify) confirmed 1 minor backward-compat gap: the initial design de-duped images by part, which also collapsed a main-doc-only file's two-refs-to-one-image case (shifting ids). Fixed by allocating one `MediaId` per relationship (`build_into`, no dedup) — main-doc behavior byte-identical to before; regression-tested. |
| P1A-030 | Fix ruby phonetic guides (`w:ruby`) | Claude Code | In review | Ruby slice of P1A-019 (`46-RUBY-PHONETIC-GUIDES-DESIGN.md`, accepted 2026-07-25), closing the P1A-025 ruby reorder/merge finding. **No model change** — import-only. **Import** (`casual-doc-import`): `in_ruby_annotation` flag (set on `w:rt`, cleared+reported on `</w:rt>`, saved/restored across text-box frames, reset at paragraph close) suppresses the annotation run text so the base (`w:rubyBase`) reads in document order; the annotation is reported (dispositioned). Full annotation modeling deferred. 2 new import tests. All gates green (fmt, clippy, 199 tests, doctest, wasm, MSRV 1.85, doc). **Adversarial review** (2 dimensions → verify) confirmed 1 minor defect: a bool flag let a nested `w:rt` (valid OOXML) clear the outer annotation early, re-merging a fragment — fixed by making it a depth counter (`ruby_annotation_depth`), regression-tested. |
| P1A-031 | Model comments (extra-part bodies + reference + metadata) | Claude Code | In review | Comments slice of P1A-019 (`47-SCHEMA-V1-COMMENTS-DESIGN.md`, accepted 2026-07-25), closing the P1A-025 comment-part silent-drop. **Model** (`casual-doc-model`): additive `Definitions.comments: DefinitionMap<CommentId, Comment>` (empty-omitted so existing snapshots + migration golden are byte-identical), `Comment{blocks, author?, initials?, date?}` (metadata retained opaque + bounded), `InlineNode::CommentReference{id, comment}`, `DanglingCommentRef` error; `validate_comments` (fresh depth; author/initials ≤255 → `comment.metadata`, date ≤64 → `comment.date`), reference resolution, id-uniqueness + snapshot-limits recurse into comments. **Import** (`casual-doc-import`): reuses the note-container state machine (`note_container == Some(b"comment")`); `import_package` reads the `/comments` part (only the canonical relationship, not `commentsExtended`/`Ids`/`Extensible`) via `resolve_part_sources` so comment-part images/hyperlinks resolve; `open_note` captures `w:author`/`w:date`/`w:initials` into `CommentMeta` (dropped, not truncated, when empty/oversized); `parse_comments`/`build_comments` map `w:id`→`CommentId` (document order, after footers, before body); body `w:commentReference` (in a run) → `CommentReference` (dangling reported); `w:commentRangeStart`/`End` reported (deferred). Fidelity harness: `CommentReference` renders no in-flow text. 10 new tests (3 model + 7 import incl. metadata/dangling/text-box/oversized-metadata + 2 truncated-table regressions). All gates green (fmt, clippy, 212 tests, doctest, wasm, MSRV 1.85, doc). **Adversarial multi-agent review** (5 dimensions → verify) confirmed 3 findings: (1) major — a table left open by EOF-truncated markup in a comment was stranded in the shared `TableStack` and dropped (and, had the XML layer not rejected the mismatched-close variant as `MalformedXml`, could have bled into the next comment); fixed with `TableStack::flush_open` called at every container boundary (`close_note`, main `parse`, `parse_header_footer`) plus a `suppressed_tbl_depth` reset — the pre-existing main-body/notes EOF gap is closed too; regression-tested (comment + body). (2) minor — a `w:commentReference` inside a footnote/endnote/header/footer part is reported but not modeled (the comment index is built after those parts); accepted-and-deferred as the same established pattern as note-references inside secondary parts (`empty_notes`), documented in `47-…` §deferred. (3) folded into (1). |
| P1A-032 | Model tracked changes / revisions (`w:ins`, `w:del`, `w:delText`) | Claude Code | In review | Final slice of P1A-019 (`48-SCHEMA-V1-TRACKED-CHANGES-DESIGN.md`, accepted 2026-07-25), closing the last semantic-import audit item. **Model** (`casual-doc-model`): additive `InlineNode::Revision{id, kind: Insertion/Deletion, author?, date?, revision_id?, inlines}` (a wrapper inline like Hyperlink/Field — not a `Run` flag — so a range's single author/date/id covers runs, hyperlinks, drawings, and nested revisions; deleted text lives verbatim in the wrapped runs), `RevisionKind`, `MAX_REVISION_DEPTH=8`, `EmptyRevision`/`RevisionNestingTooDeep` errors; **no new id newtype and no `Definitions` field**, so serialization is byte-identical for existing snapshots + the migration golden. `validate_inlines` threads a `revision_depth`; the Revision arm enforces empty-range, nesting bound, and metadata domains (author/revision_id ≤255 → `revision.metadata`, date ≤64 → `revision.date`), and is transparent to `in_wrapper` (a revision may wrap a hyperlink/field and sit inside one). Id-uniqueness + snapshot limits recurse. **Import** (`casual-doc-import`): the two mutually-exclusive wrapper singletons (`hyperlink`/`field`) are unified with a `revisions` stack under one innermost-wins discriminator stack (`wrapper_order`), so a revision nests correctly with a hyperlink in **both** directions (revision-in-link and link-in-revision) and within itself (`ins>del`) without losing either wrapper; `push_segment` routes to `wrapper_order.last()`. `open_revision` (guarded `ppr_depth==0 && rpr_depth==0` so paragraph-mark/property-change/move revisions fall through to the report arms), `w:delText` captured like `w:t`, `finish_paragraph` drains wrappers innermost-first, `ContentFrame` saves/restores the new stacks. Fidelity harness recurses revisions. 24 new tests (11 model + 13 import incl. both hyperlink-nesting directions, both text-box-nesting directions, delText, nested ins>del, deferred-revision reporting, empty/oversized/EOF-unclosed + 2 stack-desync regressions). All gates green (fmt, clippy, 232 tests, doctest, wasm, MSRV 1.85, doc). **Adversarial multi-agent review** (5 dimensions → verify) confirmed 3 findings, all fixed: (major, two dimensions, one root cause) the `on_end` `w:ins`/`w:del` arm had no close-side counter, so an *excluded* inner range's close (a property-context `w:rPr>w:ins` marker, or an over-`MAX_REVISION_DEPTH` range) prematurely committed the *enclosing* real revision — desyncing the wrapper stack and either misplacing tracked content one level or dropping the revision wrapper and emitting deleted/inserted text as plain runs. Fixed with a `suppressed_revision_depth` close-side counter (mirroring `field_depth`/`hyperlink_depth`/`suppressed_tbl_depth`): the `on_start` arm is now unconditional (open a real range or report-and-count), and the `on_end` arm balances the counter before any commit; regression-tested (property-context marker inside an open `w:del`, and 9-deep nested `w:ins`). (minor) the model validated `revision_id` at ≤255 but the importer/contract bound is ≤64; fixed to ≤64 (author stays ≤255), regression-tested. |
| P1A-033 | Model run-property long tail (toggles + fonts + vocabularies) | Claude Code | In review | First property-long-tail slice (`49-SCHEMA-V1-RUN-PROPERTIES-DESIGN.md`), prioritized by the P1A-0AA coverage audit (rFonts #1, highlight #4, vertAlign #5, caps #8). **Model** (`casual-doc-model`): additive `RunProperties` fields — `all_caps`/`small_caps`/`hidden`/`web_hidden`/`double_strike` (toggles); `font_ref` finally populated as the `w:rFonts@ascii` slot plus `font_ref_h_ansi`/`font_ref_cs`/`font_ref_east_asia`; `vertical_alignment`/`highlight`/`emphasis` with new closed enums `VerticalAlignment`/`HighlightColor`/`EmphasisMark`. All `Option`, `skip_serializing_if` only (matches convention; serde auto-defaults missing Option) → default serializes to `{}`, existing snapshots + migration golden byte-identical. `check_run_property_refs` bounds all four font slots. **Import** (`casual-doc-import`): `apply_run_property` arms for the toggles, `rFonts` (theme-first slot resolution; `false`/reported when no slot resolves — no silent swallow of an `@hint`-only element), and the three vocabularies (unknown `@val` reported like `sz`). **Fix folded from adversarial review**: `migration.rs` `run_properties_from_marks` total-struct-literal ended with `..RunProperties::default()`. 7 new tests (3 model + 4 import). All gates green (fmt, clippy, 181 tests, doctest, wasm, MSRV 1.85, doc). Deferred (design captured): run metrics (`w:spacing`/`kern`/`position`) + `w:lang`. |
| P1A-034 | Model paragraph-property long tail (flags + outline; wave 1) | Claude Code | In review | Paragraph-property slice (`50-SCHEMA-V1-PARAGRAPH-PROPERTIES-DESIGN.md`), audit #6. **Scope (wave 1):** the toggle flags `keepNext`/`keepLines`/`pageBreakBefore`/`widowControl`/`contextualSpacing`/`suppressLineNumbers` (bools) and `outlineLvl` (0..=9) — all additive `ParagraphProperties` fields mapped through the shared `apply_paragraph_property`, so they work in BOTH the body and the styles pPr state machines. `validate` bounds `paragraph.outline_level`. Backward-compat: bools `skip_serializing_if` Not::not, outline Option — a default `ParagraphProperties` still serializes to `{}` (guarded). Unknown `outlineLvl` reported. 5 new tests (2 model + 2 import + default-{}). All gates green (fmt, clippy, 191 tests, doctest, wasm, MSRV 1.85, doc). **Deferred to wave 2:** paragraph shading (`w:shd` — the paragraph-mark-rPr disambiguation) + borders (`w:pBdr`) + tabs (`w:tabs`, child capture). | Designed (`50-SCHEMA-V1-PARAGRAPH-PROPERTIES-DESIGN.md`) via the coverage workflow; adversarially reviewed (sound-with-fixes). Adds pBdr/shd/tabs/keepNext/keepLines/widowControl/outlineLvl/contextualSpacing to `ParagraphProperties`. **Review fixes to fold at impl**: reuse the Border/Shading value type from P1A-035; the `styles.rs` SECOND pPr state machine must also map the new props; cap `tabs` at import to match validation; filter `w:pos`. Pending implementation. |
| P1A-035 | Model table-property long tail (attribute-based; wave 1) | Claude Code | In review | Table/row/cell property slice (`51-SCHEMA-V1-TABLE-PROPERTIES-DESIGN.md`), the audit's #2/#3 priority. **Scope (wave 1):** the ATTRIBUTE-based properties — no child-element capture, so the reviewer's `top`/`bottom`/`start`/`end` border-container collision hazard is entirely avoided. **Model** (`casual-doc-model`): additive `TableProperties{alignment, width_twips, layout, look, shading}` on `Table`, `TableRowProperties{height, cant_split, header}` on `TableRow` (both `skip_serializing_if` empty → existing snapshots + migration golden byte-identical), extended `TableCellProperties` with `shading`/`vertical_alignment`/`no_wrap`/`text_direction`; new types `Shading`, `TableLayout`, `TableLook`, `HeightRule`, `RowHeight`, `CellVerticalAlignment`, `TextDirection` (cell vAlign named `CellVerticalAlignment` to avoid the run `VerticalAlignment`); `validate_table` bounds `table.width` + `table.row.height`. **Import** (`casual-doc-import`): `tblpr_depth`/`trpr_depth` parser states (saved/restored across text-box frames, reset defensively on `tr`/`tc` open — matching `tcpr_depth`); `w:tblPr` arms (jc start/center/end→`table_alignment`, tblW dxa, tblLayout, tblLook explicit-attrs-or-legacy-hex-mask, shd fill), `w:trPr` arms (trHeight+hRule, cantSplit, tblHeader), extended `w:tcPr` arms (shd, vAlign, noWrap, textDirection); `is_true`/`parse_rgb` made `pub(crate)`. No silent loss: pct/auto widths, table justify, unknown vAlign/textDirection/layout, and patterned `w:shd` (fill still captured) all reported. 5 new tests (2 model + 3 import). All gates green (fmt, clippy, 186 tests, doctest, wasm, MSRV 1.85, doc). **Deferred to wave 2** (`P1A-035b`): borders (`tblBorders`/`tcBorders`) + margins (`tblCellMar`/`tcMar`) — the nested edge-child capture with the container-collision handling, its own slice + review. |
| P1A-036 | Model bookmarks (`w:bookmarkStart/End`) | Claude Code | Done | Implemented (`52-SCHEMA-V1-BOOKMARKS-DESIGN.md`). `Bookmark{name}` definition table (`Definitions::bookmarks`, keyed by `BookmarkId`) + paired `InlineNode::BookmarkStart`/`BookmarkEnd` markers; marker→definition validated (`DanglingBookmarkRef`), internal-hyperlink anchor resolution kept lax. Import accumulates bookmarks across body + notes + headers + footers + comments into one map; block-level markers and column bookmarks (`w:colFirst`/`w:colLast`) reported. **Review fixes folded**: split match arms (distinct start/end types); column bookmarks reported; fidelity-tool exhaustive `InlineNode` match extended; owned `&mut DefinitionMap` accumulator (not `media::build_into`); self-closing Empty-event `w:id` balancing via a source-id→`BookmarkId` map. Additive — migration golden byte-identical. Gates green (fmt/clippy/test/wasm). |
| P1A-037 | Model content controls (`w:sdt`) | Claude Code | Ready | Designed (`53-SCHEMA-V1-CONTENT-CONTROLS-DESIGN.md`); reviewed (sound-with-fixes). Wrapper block + inline node carrying sdtPr (alias/tag/id/control-kind). **Review fixes (HARD)**: block-sdt `validate_block` must restart the table budget (like text boxes) or deep tables abort; text-box `frames.len()` depth must not conflate with sdt frames; an unclosed inline sdt must drain via the `wrapper_order` stack without cross-paragraph desync; `enter_sdt_block` must `unwrap_or_default()` not `.expect()` (no panic on malformed markup); `w:docPartObj`/`w:docPartList` collapsing to one kind must still report the lost distinction. Pending implementation. |
| P1A-0AA | Model-coverage audit + parallel design (multi-agent) | Claude Code | Done | Multi-agent workflow (1 audit + 5 design + 5 adversarial-verify agents, 11 total, 0 errors): data-drivenly ranked every reported-not-modeled construct by corpus frequency × edit-value × effort, and produced adversarially-reviewed designs for the run/paragraph/table property tails, bookmarks, and content controls. Confirmed **no silent loss anywhere** (unmapped run/para props return `false` → reported; unmapped elements hit the generic `report(local)`). Top ranks: rFonts (79 corpus occ.), tcBorders/vAlign (24 each), tblW/tblInd/tblLayout, highlight, vertAlign. Drove P1A-033..037. Designs: docs 49–53. |
| P1A-019 | Schema v1 semantic extension design (model everything) | Claude Code | Designing | Multi-agent blueprint (parallel per-construct specs → synthesis → adversarial verify across backward-compat/serde, determinism/validation, R4 reconciliation) to model the remaining WordprocessingML constructs as first-class, editable v1 values while they keep round-tripping via Retention: **tables** as structure (grid/rows/cells holding nested blocks; gridSpan/vMerge; depth-bounded), **hyperlinks** (inline wrapping runs, external `r:id`/internal `w:anchor` target), **fields** (`w:fldSimple` + complex `w:fldChar` sequence), **headers/footers** (extra parts resolved via the relationship graph), **inline drawings** (`w:drawing`/`a:blip@r:embed` → `MediaId`). Additive + backward-compatible with accepted schema v1. Tables first. Feeds the semantic-mapping slices below. |
| P1A-018 | Round-trip corpus expansion (real-producer construct families) | Claude Code | In review | Grow the verified round-trip corpus to five distinct real-producer families, each a LibreOffice-valid `.docx` with an `export` round-trip test (import Retention → `write_package` → reopen → identical model + byte-identical parts): tables/lists, **nested tables + embedded image** (media mapped, image binary reproduced), **external + internal hyperlinks** (100% text fidelity), **header/footer** extra parts, **footnotes** extra part. Injected-then-LibreOffice-normalized fixtures for header/footer/footnotes guarantee producer validity. Proves "everything round-trips, everything tested" across construct families before per-construct modeling lands. 7 export tests; manifest checksums enforced. |
| P1A-017 | No-edit DOCX writer (round-trip reconstruction) | Claude Code | In review | New `casual-doc-export` crate: `write_package(&RetainedSource)` reconstructs a valid DOCX from the retained parts (deterministic, fixed timestamp, byte-identical part contents) — the accepted "exact no-op return". Makes round-trip end-to-end verifiable: import(Retention)→write→reopen→identical model. 3 tests incl. a real tables+lists document round-trip. Everything round-trips (every tag/nesting/graphic) via Retention regardless of semantic modeling; the semantic writer (re-emit after edits) is Phase 2. |
| P1A-016 | Media reference import (image relationships) | Claude Code | In review | Map the main document's `/image` relationships → `Definitions.media` (`MediaReference` with relationship id, content type from `[Content_Types].xml`, resolved part name; no bytes decoded), deterministic `MediaId`, out-of-domain fields dropped+reported. Completes the doc-32 in-scope semantic parts. `media.rs`. 1 test. |
| P1A-015 | Body section geometry import (w:sectPr) | Claude Code | In review | Map the body-level `w:sectPr` → `SectionBoundary` (page size, margins with left/right→start/end, columns), defaulted + domain-clamped, deterministic `SectionId`. Per-paragraph section breaks still reported (deferred). 1 test. README status refreshed. |
| P1A-014 | Numbering import (numbering.xml + w:numPr) | Claude Code | In review | Parse the numbering part into v1 abstract/instance definitions (OOXML abstractNumId/numId string ids → deterministic v1 ids; levels ilvl/start); resolve `w:numPr` to `ParagraphProperties.numbering` (instance + level, dangling/undefined dropped+reported). `numbering.rs` module. 2 tests. |
| P1A-013 | Package-level retention | Claude Code | In review | `import_package` in Retention mode retains every admitted part verbatim (`RetainedSource.parts`) — full package byte floor for round-trip. Bounded, fail-closed. 1 test. |
| P1A-012 | Retention mode (round-trip tier-1 byte floor) | Claude Code | In review | First round-trip slice: `ImportMode::{Semantic,Retention}`. In Retention mode the importer keeps the original main-document bytes verbatim (`RetainedSource`, D5 tier-1) so unmapped constructs are dispositioned `preserved` and an unedited document reproduces byte-for-byte; bounded + fail-closed (`retained_bytes`). Semantic stays the default (report-and-drop). 2 tests. Next: tier-2 per-construct provenance spans, package-level source snapshot in `import_package`, and the Phase-2 writer. |
| P1A-011 | Styles import + paragraph direct formatting | Claude Code | In review | `casual-doc-import`: map `w:pPr` direct formatting (alignment/indentation/spacing) and parse the styles part (`word/styles.xml` via the relationship graph) into `Definitions.styles` — paragraph/character styles with `basedOn` inheritance (dangling/kind-mismatch dropped + reported), OOXML string style-id → deterministic v1 `StyleId` mapping, and resolved `w:pStyle`/`w:rStyle` references. Added `v1::Document::new()` + `DefinitionMap::insert()`. 15 tests incl. end-to-end with a styles part. All gates green. |
| P1A-010 | Semantic WordprocessingML body import | Claude Code | In review | New `casual-doc-import` crate (ADR-011): bounded `quick-xml` parse of `word/document.xml` → deterministic `v1::Document`. Maps paragraphs, runs, text (xml:space), tab/break inlines, and direct run properties (bold/italic/underline/strike/size/RGB color); merges adjacent equal-property runs; flattens table-cell paragraphs into the body (R4). Unmapped constructs (styles, numbering, sections, tables, fields, etc.) recorded in a deterministic dual-axis compatibility report — no silent loss. DTD-rejecting, element/depth/text-bounded. `import_package()` gives full `.docx`→model. 8 tests incl. end-to-end. All gates green. Follow-ups: style/numbering/section mapping, provenance emission. |
| P1A-009 | Source package snapshot (Tier-1 provenance) | Claude Code | In review | `casual-doc-ooxml`: `source_snapshot()` returns a deterministic `SourcePackageSnapshot` (ordered part manifest with content types + sizes/compression, main document, relationship graph). No decompressed text. Byte-stable; 1 new test; all gates green. Hashing/part-level rels are follow-ups. |
| P1A-008 | Normalized schema v1 design + implementation | Claude Code | In review | `38-…` accepted 2026-07-24; implemented in `casual-doc-model` as additive `pub mod v1` (v0 root untouched → zero downstream breakage). Multi-agent blueprint (parallel specs → synthesis → adversarial verify) drove the design; all 5 verification fixes folded in (FontRef struct variant, `serde(default)` on overrides, seed-conditional determinism, empty-extensions rejection, canonical idempotence). Types + strict serde + validation (id-uniqueness, dangling refs, basedOn cycle/kind, numbering levels, property domains, adjacent runs) + deterministic total v0→v1 migration + 12 tests incl. byte-exact golden. All gates green. |
| P1A-007 | Content-types + main-document relationship graph | Claude Code | In review | `casual-doc-ooxml`: retain parsed `[Content_Types].xml` (`content_type()` accessor, override-then-default); resolve the main document's part-level relationships (`<dir>/_rels/<name>.rels`) into `DocumentRelationship` (id, type, raw target, `TargetMode` internal/external, resolved part), base-relative resolution with root-escape rejection; external targets never resolved/fetched; missing `_rels` → empty. 2 new tests; all gates green. |
| P1A-004 | Phase 1A design reconciliation | Claude Code | Designing | Multi-agent design-readiness assessment (verdict: accept-with-changes, 6 blockers) + adversarial verification (1 blocker + 4 minors, all fixed). Added `35-DISPOSITION-TAXONOMY.md` (dual-axis), `36-ADR-027-ACCEPTANCE-RECORD.md` (D1–D11 + reconciliations R1–R4), and `37-PHASE-1A-DECISION-RESEARCH.md` (MS Word / ONLYOFFICE / LibreOffice, cited); amended ADR-007; renamed Round-trip→Retention. Competitive research (Word/ONLYOFFICE/LibreOffice, cited in doc 37) resolved R1 (relationship-based main-doc discovery, relax admitter), R4 + R2 (hybrid flatten-then-preserve tables; run boundaries as offset spans before merge), D8 (accept Strict+Transitional, normalize at decode), D4 (Semantic+Retention modes, doc-21 ceilings, fail-closed ODC-1003), D5 (two-tier anchor: whole-part snapshot + content-relative offset spans, never node IDs), D9 (per-class security-conservative policy: inert macros, invalidate signatures, opaque-bounded OLE, external non-fetch ODC-1005). D1/D2/D3/D6/D7/D11 Proposed. Pending owner sign-off to flip Proposed→Accepted: D4/D5/D8/D9/D10 (+ D1/D2/D3/D6/D7/D11). Open follow-ups: R3 deterministic ID-seed; code changes for R1/R2/R4; add nested-package recursion-depth row to doc 21; schema v1 offset-unit choice. |

## Completed Work

| ID | Title | Completed | Notes |
| --- | --- | --- | --- |
| F-001 | Repository bootstrap docs | 2026-07-24 | Added root docs, initial license, agent instructions, process docs, CI gates, tracker, competitive analysis, UX/bug hunting, and docs maintenance. |
| F-002-F-010 | Foundation design batch | 2026-07-24 | Finalized glossary, support, CI, workspace, errors, limits, schema v0, fixture corpus, ADRs, and competitive pass 1. |
| P0-001 | Deterministic model transaction slice | 2026-07-24 | Added three-crate Rust workspace, atomic grapheme insertion, public snapshots/errors, 10 unit tests, doc test, WASM/MSRV checks, and CI/security policy. |
| P0-002 | Transaction semantics and history | 2026-07-24 | Added delete/split/join operations, mapping steps, semantic inverses, SDK undo/redo, stable history error, and atomicity coverage. |
| P0-003 | Normalized snapshot loading | 2026-07-24 | Added strict bounded JSON v0 load/export, semantic limits, duplicate/unknown rejection, redacted SDK errors, and collision-safe imported editing. |
| P0-004 | Selection foundation | 2026-07-24 | Added canonical directed session selection, strict revision/position validation, atomic edit/history mapping, and a fourth focused workspace crate. |
| P0-005 | Runtime event foundation | 2026-07-24 | Added bounded future-only event subscriptions, stable sequence ordering, explicit lag gaps, transaction/selection causes, and atomic journal publication. |
| P0-006 | DOCX package reader | 2026-07-24 | Added bounded ZIP preflight, safe path and codec policy, cancellable verified part reads, five generated fixtures, and CI checksum enforcement. |
| P0-007 | Benchmark and baseline harness | 2026-07-24 | Added four deterministic release workloads, typed reports, named-environment regression comparison, an Apple M4 baseline, and required CI smoke. |
| P0-008 | Phase 0 corpus and evidence closure | 2026-07-24 | Added two package fixtures, exact corpus policy, independently locked package-reader fuzzing, scheduled security coverage, and an accepted exit report backed by a green 12-check matrix. |
| F-011 | Phase 1 capability decomposition | 2026-07-24 | Split semantic import, typography/layout, pagination/display list, and rendering/hit testing into independently gated stages. |
| F-012 | Apache-2.0 license policy | 2026-07-24 | Adopted Apache-2.0 for the whole project before accepting external contributions. |

## Open Questions

- Should the crate family retain the `casual-doc-*` names if public package-name availability later requires a change?
- Which fixed font set should be used for deterministic layout baselines?
